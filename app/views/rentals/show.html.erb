<div class="container card justify-content-center text-center my-5 w-50 p-5 rounded rounded-4">
  <h2>Your total cost will be <%= @rental.total_price.to_i %> $</h2>
  <h1>Would like to continue?</h1>

  <%= button_to(
          'Confirm Rent',
          car_rental_path(@car, @rental),
          class: 'btn btn-danger text-white rounded rounded-4 mt-3',
          method: :patch,
          params: { confirmation: true }
        )%>
</div>
