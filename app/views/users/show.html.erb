<div class="container">
  <%@cars.each do |car|%>
    <%if @user.id == current_user.id%>
      <%= image_tag car.image_url, width: 200, height: 160%>
      <%=car.year%>
      <%=car.brand%>
      <%=car.model%>
      <%= link_to "See details", car_path(car), class: "btn btn-primary" %>
      <% end %>
  <% end %>
</div>
<div class="container">
  <%@user.rentals.each do |rental|%>
    <%= image_tag rental.car.image_url, width: 400, height: 300%>
    <%=rental.start_date%>
    <%=rental.end_date%>
  <% end%>
</div>
